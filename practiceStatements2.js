//My example.function name was mispelled hence the error message function undefined. after rectifying. it produced correct output

function calculateInterest (pricipal, rate, years) {
  let  calculateInterest = pricipal* rate * years
    return calculateInterest
}
 let totalInterest =calculateInterest(1000, 0.03, 3)
 console.log("The intrest is:" ,totalInterest)


 // functions_practice.js, coursework example
/*function calculateInterest(principal, rate, years) {
  // Calculate the interest
  let interest = principal * rate * years;

  // Return the calculated interest
  return interest;
}
// Call the function with the required values
let totalInterest = calculateInterest(1000, 0.05, 3);

// Print the total interest earned to the console
console.log("Total interest earned:", totalInterest);*/
